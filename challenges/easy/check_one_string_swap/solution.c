#include <stdbool.h>

bool areAlmostEqual(char* s1, char* s2) {
    (void)s1;
    (void)s2;

    /*
     * Return true if the strings can be made equal with at most one swap
     * inside one of the strings.
     */

    return false;
}
