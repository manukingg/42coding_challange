int minIncrementForUnique(int* nums, int numsSize) {
    (void)nums;
    (void)numsSize;

    /*
     * Return the minimum total number of increments needed so that every
     * value in nums becomes unique.
     */

    return 0;
}
